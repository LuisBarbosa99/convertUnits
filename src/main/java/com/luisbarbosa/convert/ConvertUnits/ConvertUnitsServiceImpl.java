package com.luisbarbosa.convert.ConvertUnits;

public class ConvertUnitsServiceImpl {

/*
    public void start(String equacao){


        String nomesSI = equacao;
        List<String> nomes = Banco.getTodosNomesSimbolos();

        for (String name:nomes){
            equacao.replace(name, Banco.getValueName(name));
            nomesSI.replace(name, Banco.getSI(name));
        }


        if (equacao.contains("*") || equacao.contains("/")){

            if (!equacao.contains("(")){
                resposta = calculo(subEquacao);
            }else {

                while(true){
                    subEquacao = openClose(equacao);

                    if (subEquacao == equacao)
                        break;

                    resposta = calculo(subEquacao);
                    subEquacao = "(" + subEquacao + ")";

                    equacao.replace(subEquacao, Float.toString(resposta));
                }
            }
        }else{
            resposta = Float.parseFloat(equacao);
        }
    }
*/

}
